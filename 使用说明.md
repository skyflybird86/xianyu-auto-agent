# 闲鱼自动回复机器人 - 使用说明

## 📦 文件清单

### 方式一：直接运行（macOS）
解压后会看到以下文件：
```
xianyu-test/
├── 闲鱼自动回复机器人      # 主程序（双击运行）
├── chrome-extension/       # Chrome浏览器插件
│   ├── manifest.json
│   ├── popup.html
│   ├── popup.js
│   ├── content.js
│   └── icons/              # 插件图标
└── 使用说明.md             # 本说明文档
```

### 方式二：源码打包（Windows/macOS）
解压后会看到以下文件：
```
xianyu-source/
├── main.py                 # 主程序入口
├── web_server.py           # Web服务器
├── build.bat               # Windows打包脚本
├── build.spec              # 打包配置
├── requirements.txt        # 依赖列表
├── .env                    # 配置文件
├── activation_client.py    # 激活验证模块
├── activation_merchant.py  # 激活码生成工具
├── chrome-extension/       # Chrome插件
├── prompts/                # 提示词模板
├── data/                   # 数据目录
└── 使用说明.md             # 本说明文档
```

---

## 🚀 快速开始

### 方式一：直接运行（macOS）

**macOS用户**：
```bash
cd xianyu-test
chmod +x 闲鱼自动回复机器人
./闲鱼自动回复机器人
```

### 方式二：源码运行（Windows）

**Windows用户（推荐方式）**：
1. 解压 `xianyu-source.zip`
2. 双击运行 `run.bat`
3. 等待依赖安装完成
4. 程序自动启动

**Windows用户（打包方式）**：
1. 解压 `xianyu-source.zip`
2. 双击运行 `build.bat`
3. 等待打包完成
4. 在 `dist` 文件夹中找到 `闲鱼自动回复机器人.exe`
5. 双击运行

**macOS用户**：
```bash
cd xianyu-source
chmod +x build.sh
./build.sh
```

程序启动后会显示：
```
Web服务器已启动，访问 http://localhost:5000
```

### 第二步：打开Web管理页面

打开浏览器访问：**http://localhost:5000**

页面包含以下功能区域：
- **服务状态** - 查看连接状态、Token状态、心跳状态
- **系统配置** - 设置API Key、Cookie等参数
- **使用说明** - 详细的使用指南
- **Chrome扩展安装指南** - 一键获取Cookie
- **服务日志** - 查看运行日志

### 第三步：配置参数

1. **在Web页面中找到"系统配置"区域**
2. **填写API Key**（从阿里云百炼平台获取）
3. **填写Cookie**（使用Chrome插件一键获取）
4. **点击"保存配置"按钮**

### 第四步：启动自动回复服务

1. **在Web页面中找到"服务状态"区域**
2. **点击"🚀 启动自动回复服务"按钮**
3. **等待显示"服务启动成功"提示**

---

## 🎯 Chrome插件安装（推荐）

### 安装步骤：

1. **打开Chrome浏览器**
2. **地址栏输入**：`chrome://extensions/`
3. **开启"开发者模式"**（右上角开关）
4. **点击"加载已解压的扩展程序"**
5. **选择 `chrome-extension` 文件夹**
6. **浏览器右上角出现小鱼图标 🐟**

### 使用方法：

1. **打开闲鱼网站**（https://www.goofish.com）
2. **登录账号**
3. **点击小鱼图标**
4. **点击"获取Cookie"按钮**
5. **点击"打开配置页面"**（Cookie自动填充）

---

## 🌐 Web页面功能

### 服务状态
- 显示连接状态（在线/离线）
- 显示Token验证状态
- 显示心跳状态
- 显示运行时间
- 统计消息处理数量、活跃会话数

### 服务控制
- **启动服务** - 开始自动回复
- **停止服务** - 停止自动回复
- **重启服务** - 重新启动服务

### 系统配置
- API Key（通义千问密钥）
- Cookie字符串（闲鱼登录凭证）
- 模型接口地址
- 模型名称
- 人工接管关键词
- 模拟人工输入开关
- 心跳间隔
- Token刷新间隔

### 服务日志
- 实时查看运行日志
- 按级别过滤（DEBUG/INFO/WARNING/ERROR）
- 清空日志

---

## ⚠️ 常见问题

### 1. 提示"COOKIES_STR 未配置"

**解决**：在配置页面填写Cookie，然后点击"保存配置"，再启动服务。

### 2. 提示"Cookie中缺少必要的'unb'字段"

**解决**：
- 确保已登录闲鱼账号
- 使用Chrome插件获取Cookie
- 或手动登录闲鱼后刷新页面再获取

### 3. Web页面无法打开

**解决**：
- 检查程序是否正在运行
- 检查端口5000是否被占用
- 尝试访问 http://localhost:8080

### 4. 服务启动失败

**解决**：
- 查看日志页面获取详细错误信息
- 检查API Key和Cookie是否正确
- 确保网络连接正常

---

## 🔑 测试激活码

```
XY2026-AAAA-BBBB-CCCC
```

---

## 📞 技术支持

如有问题，请联系技术支持。
