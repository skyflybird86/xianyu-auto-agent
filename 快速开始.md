# 闲鱼自动回复机器人 - 快速开始

## 📋 前置要求

### Windows用户
1. **安装Python**（3.8+版本）
   - 下载地址：https://www.python.org/downloads/windows/
   - **安装时必须勾选 "Add Python to PATH"**

### macOS/Linux用户
- Python3通常已预装，如果没有请通过官方渠道安装

## 🚀 三种运行方式

### 方式1：启动脚本（推荐，最简单）⭐⭐⭐

#### Windows用户
1. 双击运行 `start.bat`
2. 首次运行会自动创建虚拟环境并安装依赖
3. 如果未激活，按提示输入激活码
4. 打开浏览器访问 http://localhost:8080

#### macOS/Linux用户
```bash
chmod +x start.sh
./start.sh
```

### 方式2：Docker容器（推荐给技术用户）

```bash
# 一键启动
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

### 方式3：打包成可执行文件（技术用户）

```bash
# 安装依赖
pip install pyinstaller

# 执行打包
chmod +x build.sh
./build.sh

# 打包完成后在 dist/ 目录找到可执行文件
```

## 📱 首次使用配置

1. 启动服务后，打开浏览器访问：**http://localhost:8080**

2. 配置API Key（通义千问）
   - 登录 https://bailian.console.aliyun.com/
   - 获取 API Key

3. 配置Cookie（两种方式）
   - **方式A（推荐）**：使用Chrome插件自动抓取
     1. 在Chrome中安装 `chrome-extension/` 目录下的插件
     2. 打开闲鱼网页版 https://2.taobao.com/
     3. 点击插件图标，复制Cookie
     4. 粘贴到Web界面
   
   - **方式B（手动）**：
     1. 打开浏览器开发者工具（F12）
     2. 访问闲鱼，过滑块验证
     3. 在Network面板复制完整Cookie

4. 保存配置，等待服务连接

## 🔧 功能说明

### 自动回复
- 机器人自动识别用户消息意图
- 自动回复价格咨询、技术问题等
- 可自定义提示词

### 人工接管
- 在闲鱼聊天中输入 `。` 即可接管会话
- 再次输入 `。` 恢复自动回复

### 消息去重
- 5层去重机制，防止重复回复
- 即使服务器多次广播消息，也只回复一次

### Web界面
- 实时查看运行状态
- 查看和搜索日志
- 在线配置所有参数
- 查看会话历史

## 🔐 激活码说明

### 首次激活
- 双击启动后，如果未激活会提示输入激活码
- 激活码格式：`XY2026-XXXX-XXXX-XXXX`
- 最多尝试3次，激活后绑定到当前机器

### 已激活用户
- 再次启动时会直接跳过激活验证
- 激活有效期：1年

### 获取激活码
- 联系客服获取有效的激活码

## 📋 目录结构

```
XianyuAutoAgent-main/
├── main.py                 # 主程序
├── web_server.py           # Web服务器
├── XianyuAgent.py          # AI回复逻辑
├── context_manager.py      # 上下文管理
├── shared_state.py         # 状态共享
├── activation.py           # 激活码验证
├── start.sh                # macOS/Linux启动脚本
├── start.bat               # Windows启动脚本
├── build.sh                # 打包脚本
├── build.spec              # PyInstaller配置
├── docker-compose.yml      # Docker配置
├── requirements.txt        # Python依赖
├── .env.example            # 配置示例
├── prompts/                # AI提示词
├── templates/              # Web界面模板
├── chrome-extension/       # Chrome插件
└── data/                   # 数据目录（自动创建）
```

## ❓ 常见问题

### Q1: Windows提示 "不是内部或外部命令"
- **原因**：Python未添加到系统PATH
- **解决**：重新安装Python，确保勾选 "Add Python to PATH"

### Q2: 提示 "No module named 'websockets'"
- **原因**：虚拟环境依赖未安装
- **解决**：运行 `start.bat` 会自动安装依赖，等待完成即可

### Q3: Cookie过期怎么办
- **解决**：打开闲鱼网页版，重新过滑块验证，复制新的Cookie到Web界面

## 🆘 获取帮助

- 查看Web界面日志
- 检查终端输出
- 确认API Key和Cookie配置正确
